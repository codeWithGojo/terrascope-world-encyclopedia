import type {Metadata} from "next";import {notFound} from "next/navigation";import Link from "next/link";import {SiteHeader} from "../../components/SiteHeader";import {SiteFooter} from "../../components/SiteFooter";import RankingsExplorer from "../RankingsExplorer";
const categoryMeta={economies:{title:"Largest economies",description:"Compare total economic output with definitions and source dates beside the table."},"per-capita":{title:"Highest income per person",description:"Compare average economic output per person without treating it as salary or household wealth."},population:{title:"Most populous nations",description:"See how national population estimates compare across the world."},football:{title:"Football power ranking",description:"Open the current men's national-team ranking and continue into each country dossier."},legacy:{title:"Men's World Cup legacy",description:"Compare title-winning national teams across the tournament's history."},"low-income":{title:"Lowest reported income per person",description:"Read low-output comparisons with development context and data cautions kept visible."},integrity:{title:"Highest perceived corruption risk",description:"Explore the CPI as a comparative institutional signal, not a verdict on a population."},territory:{title:"Largest countries by area",description:"Compare physical scale across TerraScope's 195-country index."}} as const;
export function generateStaticParams(){return Object.keys(categoryMeta).map((category)=>({category}));}
export async function generateMetadata({params}:{params:Promise<{category:string}>}):Promise<Metadata>{const {category}=await params;const item=categoryMeta[category as keyof typeof categoryMeta];if(!item)return {};return {title:`${item.title} ranking`,description:item.description,alternates:{canonical:`/rankings/${category}`}};}
export default async function RankingDetailPage({params}:{params:Promise<{category:string}>}){const {category}=await params;const item=categoryMeta[category as keyof typeof categoryMeta];if(!item)notFound();return <main><SiteHeader active="rankings"/><section className="ranking-detail-hero"><p className="eyebrow"><span/>Dedicated ranking view</p><div><h1>{item.title}<br/><em>in context.</em></h1><p>{item.description}</p></div><Link href="/rankings">← Back to ranking overview</Link></section><RankingsExplorer initialCategory={category} detail/><SiteFooter/></main>}
